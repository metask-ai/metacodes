diff --git a/.github/workflows/win-repro-original.patch b/.github/workflows/win-repro-original.patch
index 14dd7efa..e69de29b 100644
--- a/.github/workflows/win-repro-original.patch
+++ b/.github/workflows/win-repro-original.patch
@@ -1,31 +0,0 @@
-diff --git a/tests/component/read_state_test.zig b/tests/component/read_state_test.zig
-index a070d805..b58e5aed 100644
---- a/tests/component/read_state_test.zig
-+++ b/tests/component/read_state_test.zig
-@@ -39,12 +39,7 @@ test "L2 read_state: hashFileContent 一致 + 内容变则哈希变" {
-     // absolute with a drive letter on Windows. A bare `/tmp/...` literal is resolved by
-     // the Windows CRT against the process's current drive and is shared by every
-     // parallel shard; this test has no business depending on either.
--    var dir_buf: [512]u8 = undefined;
--    const dir = cc.util_fs.testing.perPidDir(&dir_buf, "cc-zig-read-state");
--    if (std.c.mkdir(dir.ptr, 0o755) != 0 and !pfs.exists(dir.ptr)) return fixtureFailure("mkdir", dir.ptr);
--    defer _ = std.c.rmdir(dir.ptr);
--    var path_buf: [512]u8 = undefined;
--    const p = try std.fmt.bufPrintZ(&path_buf, "{s}/cc-rs-hash.txt", .{dir});
-+    const p = "/tmp/cc-rs-hash.txt"; // REPRO: original fixed literal
-     try writeFile(p, "hello world");
-     defer _ = std.c.unlink(p);
-     const h1 = cc.core_read_state.hashFileContent(p);
-diff --git a/tests/component/skill_fork_test.zig b/tests/component/skill_fork_test.zig
-index 95eb3f40..ac09da34 100644
---- a/tests/component/skill_fork_test.zig
-+++ b/tests/component/skill_fork_test.zig
-@@ -26,7 +26,7 @@ const READ_TOOL_SSE =
- /// Windows, is resolved by the CRT against the process's current drive; neither is a
- /// property this test should depend on.
- fn fixtureRoot(buf: []u8, tag: []const u8) [:0]const u8 {
--    return cc.util_fs.testing.perPidDir(buf, tag);
-+    return std.fmt.bufPrintZ(buf, "/tmp/{s}", .{tag}) catch unreachable; // REPRO: original fixed literal
- }
- 
- fn mkdirOrExisting(path: [*:0]const u8) !void {
diff --git a/scripts/windows_test_prelude.zig b/scripts/windows_test_prelude.zig
index f574e4e5..4a3e2670 100644
--- a/scripts/windows_test_prelude.zig
+++ b/scripts/windows_test_prelude.zig
@@ -9,4 +9,10 @@ pub fn main(init: std.process.Init) !void {
     // TODO: migrate those tests to std.testing.tmpDir/platform.paths.tempDir,
     // then delete this prelude instead of preserving the root-directory hack.
     try std.Io.Dir.cwd().createDirPath(init.io, "/tmp");
+    // REPRO: stamp when the prelude ran (wall clock ns) so a shard's fixture failure
+    // can be ordered against it; the marker lives in the workspace, not in \tmp.
+    var name_buf: [64]u8 = undefined;
+    const name = try std.fmt.bufPrint(&name_buf, ".prelude-ran-{d}", .{std.Io.Clock.real.now(init.io).nanoseconds});
+    const f = try std.Io.Dir.cwd().createFile(init.io, name, .{});
+    f.close(init.io);
 }
diff --git a/tests/component/read_state_test.zig b/tests/component/read_state_test.zig
index a070d805..b1c61a06 100644
--- a/tests/component/read_state_test.zig
+++ b/tests/component/read_state_test.zig
@@ -28,8 +28,8 @@ fn fixtureFailure(op: []const u8, path: [*:0]const u8) error{FixtureIoFailed} {
     else
         "?";
     std.debug.print(
-        "read_state fixture: {s} failed path={s} errno={d} cwd={s}\n",
-        .{ op, path, std.c._errno().*, cwd },
+        "read_state fixture: {s} failed path={s} errno={d} cwd={s} now_ns={d}\n",
+        .{ op, path, std.c._errno().*, cwd, std.Io.Clock.real.now(std.testing.io).nanoseconds },
     );
     return error.FixtureIoFailed;
 }
@@ -39,12 +39,7 @@ test "L2 read_state: hashFileContent 一致 + 内容变则哈希变" {
     // absolute with a drive letter on Windows. A bare `/tmp/...` literal is resolved by
     // the Windows CRT against the process's current drive and is shared by every
     // parallel shard; this test has no business depending on either.
-    var dir_buf: [512]u8 = undefined;
-    const dir = cc.util_fs.testing.perPidDir(&dir_buf, "cc-zig-read-state");
-    if (std.c.mkdir(dir.ptr, 0o755) != 0 and !pfs.exists(dir.ptr)) return fixtureFailure("mkdir", dir.ptr);
-    defer _ = std.c.rmdir(dir.ptr);
-    var path_buf: [512]u8 = undefined;
-    const p = try std.fmt.bufPrintZ(&path_buf, "{s}/cc-rs-hash.txt", .{dir});
+    const p = "/tmp/cc-rs-hash.txt"; // REPRO: original fixed literal
     try writeFile(p, "hello world");
     defer _ = std.c.unlink(p);
     const h1 = cc.core_read_state.hashFileContent(p);
diff --git a/tests/component/skill_fork_test.zig b/tests/component/skill_fork_test.zig
index 95eb3f40..eeb1b239 100644
--- a/tests/component/skill_fork_test.zig
+++ b/tests/component/skill_fork_test.zig
@@ -26,7 +26,7 @@ const READ_TOOL_SSE =
 /// Windows, is resolved by the CRT against the process's current drive; neither is a
 /// property this test should depend on.
 fn fixtureRoot(buf: []u8, tag: []const u8) [:0]const u8 {
-    return cc.util_fs.testing.perPidDir(buf, tag);
+    return std.fmt.bufPrintZ(buf, "/tmp/{s}", .{tag}) catch unreachable; // REPRO: original fixed literal
 }
 
 fn mkdirOrExisting(path: [*:0]const u8) !void {
@@ -46,8 +46,8 @@ fn fixtureFailure(op: []const u8, path: [*:0]const u8) error{FixtureIoFailed} {
     else
         "?";
     std.debug.print(
-        "skill_fork fixture: {s} failed path={s} errno={d} cwd={s}\n",
-        .{ op, path, std.c._errno().*, cwd },
+        "skill_fork fixture: {s} failed path={s} errno={d} cwd={s} now_ns={d}\n",
+        .{ op, path, std.c._errno().*, cwd, std.Io.Clock.real.now(std.testing.io).nanoseconds },
     );
     return error.FixtureIoFailed;
 }
