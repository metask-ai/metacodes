# Development dependencies for the Python suites. Everything else is stdlib.
# Python >= 3.9 (the macOS system python3) is the floor: scripts/tests/test_python_floor.py pins it.
PyYAML>=6.0  # scripts/eval/workbuddy and its tests
