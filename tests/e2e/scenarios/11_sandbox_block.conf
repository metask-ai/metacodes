# 11_sandbox_block — 验沙箱拦 cwd 外的 Bash 写(macOS Seatbelt)
# 沙箱只拦 Bash 发起的写,所以必须用 Bash 写越界(不能用 Write/Edit)。
PERMISSION=default
SETTINGS=fixtures/sandbox.json
# cwd 内的 inside.txt 应建成;cwd 外的 /tmp/... 应被 Seatbelt 拒。
EXPECT_FILE=inside.txt
EXPECT_HARD=0
