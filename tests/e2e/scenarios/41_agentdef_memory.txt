使用 Task 工具调用 release-memory agent。让它把固定秘密 MEMORY_RELEASE_7F3A 直接写入其系统提示指定的 Persistent Agent Memory/MEMORY.md；这是全新 memory，子 agent 不应先 Read 不存在的文件。不要把内存路径作为 Task 参数传入，也不要让父 agent 代写。父 agent 在 Task 返回后不得用 Read、Glob 或 Bash 自行验证 memory 路径。
---
再次使用 Task 工具启动一个新的 release-memory agent，只要求它读取自己的 MEMORY.md，逐字复制其中以 MEMORY_ 开头的完整秘密，然后由该子 agent 亲自用 Write 把仅包含该 token 的内容写入当前工作目录下的 memory-proof.txt，最终只返回 RECALL_WRITTEN；不要在这次 Task prompt 里重复秘密内容。父 agent 不得自行读取 memory，也不得调用 Write 创建或修改 memory-proof.txt；只验收 Task 返回的 RECALL_WRITTEN。
