You are doing a security review of the `svc` package in this workspace (see README.md). Request handlers in svc/handlers.py read untrusted values from request.args and hand them to helper chains under svc/pipeline/ and svc/util/. Exactly one request-controlled value reaches a shell execution without any validation or quoting along the way. Trace every handler chain to the sink and determine which one it is. Write your review to a file named report.md in the workspace root. It must contain exactly one line of the form `FINDING: source=<module>.<function> sink=<module>.<function>` naming the handler function where the value enters and the function where it reaches the shell, followed by a short justification for that chain and a one-line note per handler you ruled out and why.
