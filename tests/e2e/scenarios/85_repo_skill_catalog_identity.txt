Audit this historical AgentCore snapshot. The public Skill catalog currently conflates catalog revision with per-Skill identity, so multiple records can resolve to the wrong Skill and SDK consumers cannot safely own or validate the descriptor. Do not edit yet. Write PLAN.md preserving these requirements: each Skill gets a lowercase 64-hex id derived from SHA-256(invocation_name); catalog_revision remains content-addressed for the whole snapshot; duplicate Skill ids fail closed; descriptor JSON exposes skill_id, invocation_name, display_name, description, argument schema, health, and issues.
---
Implement the identity contract across src/skills/runtime/catalog.zig, sdk/zig/protocol.zig, sdk/zig/types.zig, and src/agentcore/abi_v1.zig. The SDK decoder must own its decoded data, validate canonical ids and resource limits, and preserve degraded issue metadata. Extend tests/component/agentcore_abi_test.zig so two Skills such as review and workctl have distinct ids and invoke only their own body sentinel.
---
Inspect producer, SDK, ABI, and L2 tests as one end-to-end contract. Write HANDOFF.md containing these literal fields: skill_id=sha256(invocation_name), catalog_revision=content_addressed, duplicate_ids=reject, and descriptor_ownership=consumer. Do not claim success if only the descriptor shape changed but invocation still aliases records.
