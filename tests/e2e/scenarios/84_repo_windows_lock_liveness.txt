Audit this historical metacodes swarm snapshot. Under saturated in-process teammates on Windows, diagnostic reads race DeleteFile and leaked lock sentinels produce spurious LockBusy; the current retry horizon is also much shorter than stale_ms. Do not edit yet. Write PLAN.md preserving these exact constraints: stale_ms remains 10000; acquisition gets 100 retries with capped exponential backoff and jitter; Windows stale detection is mtime-only; stale stealing stays rename-linearized; release retries unlink at most 20 times with a 2ms delay and stops if the file is already gone.
---
Implement the liveness repair in src/swarm/file_lock.zig. Preserve POSIX content-based diagnostics, use the platform mtime path on Windows, retain the pre-turn setMemberActiveBestEffort scheduling point in teammate.zig, and tighten the two slow mock-server delays in tests/component/teammate_runtime_test.zig to 1500ms so deinit remains bounded. Add or retain comments explaining the non-obvious concurrency invariants.
---
Review the complete diff for lock ownership and teardown regressions. Write HANDOFF.md containing these literal audit fields: stale_ms=10000, retry_budget≈9700ms, release_retries=20, and windows_probe=mtime_only. The checked-in source, not the handoff alone, must implement the repair.
