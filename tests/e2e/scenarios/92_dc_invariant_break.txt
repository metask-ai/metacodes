Read CONTRACT.md in this workspace: every worker module under workers/ exposes run(job, ctx) and must call job.ack() exactly once on every normally-returning path, and never on a raising path; helpers may ack on run's behalf. Exactly one worker violates the contract. Audit every worker and identify it. Write your audit to a file named report.md in the workspace root. It must contain exactly one line of the form `FINDING: workers.<module>.run` naming the violating worker, followed by the offending path and a one-line verdict per worker you checked.
