# 13_perm_interactive — 验 --answers-file 预置应答放行 default 模式下的 .ask
# default 模式下 Write 触发 .ask;非 tty 无法手动应答 → 用 answers 队列预置 y/y。
PERMISSION=default
ANSWERS=fixtures/answers_13.txt
EXPECT_FILE=first.txt
EXPECT_FILE=second.txt
EXPECT_HARD=0
